{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Train (experiment=test) via uv",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/train.py",
      "args": [
        "+experiment=tune_pykan"
      ],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "env": {
        "PATH": "/Users/noah/.local/bin:${env:PATH}"
      }
    }
  ]
}