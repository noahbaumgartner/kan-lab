# @package _global_
defaults:
  - tune_base
  - override /model: pykan

hydra:
  sweeper:
    params:
      optimizer: choice(adam, lbfgs)
      model.grid: range(3, 51)
      optimizer.lr: tag(log, interval(0.001, 1.0))
      training.lamb: tag(log, interval(1e-5, 0.1))
