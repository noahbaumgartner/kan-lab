_target_: src.models.kanc_mlp_model.KANCMLPModel

num_classes: ${dataset.output_dim}
img_size: ${dataset.img_size}
in_chans: ${dataset.in_chans}

conv1_out: 5
conv2_out: 10
kernel_size: 3
hidden_dim: 100
grid_size: 5
