_target_: src.datasets.feynman.FeynmanDataset
name: "I.13.12"
n_train: 3000
n_test: 1000

task_type: regression

# metadata for width construction (not passed to __init__)
input_dim: 5
output_dim: 1

# paper shape: [2, 2, 1] — 1 hidden layer
# note: paper uses 2 inputs (Gm1m2 and 1/r2-1/r1), but we pass all 5 raw variables
n_hidden: 1
n_hidden_min: 0
n_hidden_max: 2
hidden_width_min: 1
hidden_width_max: 5
