_target_: src.datasets.feynman_i_9_18.FeynmanI918Dataset
n_train: 1000
n_test: 100

# metadata for width construction (not passed to __init__)
input_dim: 9
output_dim: 1

# paper shape: [6, 4, 2, 1, 1] — 3 hidden layers
# note: paper uses 6 inputs (differences computed internally), but we pass all 9 raw variables
n_hidden: 3
n_hidden_min: 2
n_hidden_max: 4
hidden_width_min: 1
hidden_width_max: 5
