# @package _global_
defaults:
  - tune_base
  - override /model: mlp
  - override /training: default

experiment_name: tune-mlp

hydra:
  sweeper:
    params:
      model.n_hidden_layers: range(1, 5)
