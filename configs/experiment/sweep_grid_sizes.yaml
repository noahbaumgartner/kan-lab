# @package _global_
defaults:
  - /model: pykan
  - /dataset: xy
  - /training: default

experiment_name: grid-size-sweep

hydra:
  sweeper:
    params:
      model.grid: 3,5,10,20,50
      model.width: choice([2,3,1],[2,5,1],[2,5,5,1])
