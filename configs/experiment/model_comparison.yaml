# @package _global_
defaults:
  - override /dataset: sincos
  - override /training: default

experiment_name: model-comparison

hydra:
  sweeper:
    params:
      model: pykan,efficient_kan,mlp,fast_kan
