# @package _global_
defaults:
  - override /dataset: bessel
  - override /model: pykan
  - override /training: default

experiment_name: train-bessel

model:
  width: [1, 1]
  grid_refinement_schedule: [3, 5, 10, 20, 50, 100, 200, 500, 1000]
  grid_refinement_steps: 200

training:
  steps: 1800
