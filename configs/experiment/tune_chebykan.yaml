# @package _global_
defaults:
  - tune_base
  - override /model: chebykan
  - override /training: adam

experiment_name: tune-chebykan

hydra:
  sweeper:
    params:
      model.degree: range(2, 9)
      training.optimizer: choice(Adam, LBFGS)
